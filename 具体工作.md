<!-- 面向 SCI 二区通信期刊的 META_HRL 新手可执行工作路线。 -->
# 面向 SCI 二区投稿的具体工作路线

本文档把“将 META_HRL 做到可投稿”拆成一条可以逐项完成的路线，面向第一次做通信强化学习论文的读者。每一步都说明：为什么做、需要改什么、如何运行、应产生什么文件，以及如何判断该步是否完成。

目标是达到通信与智能无线方向 SCI 二区期刊通常要求的实证可信度，例如 IEEE Access、OJ-COMS/OJ-VTS、Physical Communication 等相近定位期刊。它不能保证录用；选刊分区、创新性、实验质量、写作和审稿意见都会影响结果。

## 0. 先明确主线，避免走偏

### 0.1 论文应该证明什么

当前最稳妥的主张是：

> 在混合近远场大规模 MIMO-RSMA 系统中，使用双时间尺度 HRL，将慢变化的用户分组交给 Manager、将快变化的组 RSMA 资源分配交给 Worker；通过可行性投影保证功率和公共率约束，并在公平基线上验证性能、鲁棒性和计算开销。

简单理解：不是只让 PPO “学会分功率”，而是让它学会两件不同速度的事。

- Manager：不必每个时隙都换分组，而是在较慢的时间尺度选择用户如何组成组。
- Worker：在已确定分组下，每个时隙分私有流功率、组公共流功率和组内公共率。

论文最终必须回答一个明确问题：**学习得到的 Manager + Worker 是否比固定分组、启发式分组和不分层方法更好？更好体现在哪些预先定义的指标上？代价是多少？**

### 0.2 目前不要把 Meta-RL 作为核心卖点

仓库已有的配对实验显示，learned-context 分支在 validation 和 OOD 上弱于 raw deterministic context。这个结论本身是有价值的，但不能写成“Meta-RL 明显提升性能”。

本路线默认选择：**先完成一篇以 HRL 为主、Meta context 作为探索性消融的论文。**只有当重新设计的 Meta 方法在独立 OOD 设置下稳定胜出时，才将 Meta-RL 写入标题、摘要和核心贡献。

### 0.3 完成标准

在开始长时间训练前，请把下面这句话写入自己的实验笔记：

```text
主要指标：平均和速率（mean sum rate）。
约束指标：QoS 满足率、最小用户速率、Jain 公平性、平均分组切换次数。
主比较：学习 Manager + PPO Worker 对比 启发式 Manager + 同一 PPO Worker。
统计单位：训练随机种子；测试场景对各方法完全一致。
```

## 1. 开始前：搭好可复现实验环境

这一阶段不改算法，目标是确保你能运行现有代码、定位错误，并且每次实验都有记录。

### 步骤 1.1：安装依赖

在项目根目录执行：

```bash
python --version
python -m pip install -e ".[dev]"
python -m pytest
```

**应看到什么：** Python 版本至少为 3.10；pytest 全部通过。若某个测试失败，先运行单个失败测试，例如 `python -m pytest tests/test_group_rsma.py -q`，不要直接开始新功能。

**常见问题：**

- `ModuleNotFoundError`：通常是没有在项目根目录安装，或使用了不同 Python 环境。
- Stable-Baselines3/PyTorch 安装失败：先确认 Python 版本和本机 CPU/GPU 环境。
- 训练测试很慢：先用 `-q` 做接口检查，不要跳过物理层测试。

### 步骤 1.2：认识三个层级的代码入口

| 目标 | 先看文件 | 需要理解 |
| --- | --- | --- |
| 物理模型 | `src/meta_hrl/channel/`、`src/meta_hrl/rsma/` | 信道、预编码、SINR、速率 |
| 强化学习环境 | `src/meta_hrl/envs/` | 状态、动作、奖励、终止 |
| 实验流程 | `src/meta_hrl/training/`、`src/meta_hrl/cli.py` | 训练、评估、保存、分析 |

推荐先读 `代码操作指南.md`，再读本文件。训练器只是把环境交给 PPO；研究结论主要由环境、基线和评估协议决定。

### 步骤 1.3：建立实验目录

```text
outputs/paper/
  00_smoke/  01_fixed_manager/  02_heuristic_manager/
  03_learned_manager/  04_ablations/  05_scaling/
  06_robustness/  tables_and_figures/
```

每个实验目录至少保存配置、训练/测试 seed、checkpoint、原始 JSON 和运行命令。不要让不同实验共用同名 checkpoint。

**本阶段完成标志：** 全量测试通过；你能说清三个环境分别用于什么；随机 Worker 可以完成一次 `reset -> set_manager_action -> step`。

## 2. 冻结第一个可比较的问题

先只研究一个完整的小问题，不要同时改变天线数、用户数、QoS、CSI 和网络结构。

### 步骤 2.1：固定第一轮参数

| 参数 | 建议值 | 代码位置 |
| --- | --- | --- |
| 天线、用户 | `M=128`、`K=6` | `ScenarioSamplerConfig` |
| 阵列、频率 | ULA、28 GHz | `configs/channel/rician_near_far.yaml` |
| 私有预编码 | RZF | `HierarchicalRSMAEnvConfig.private_scheme` |
| 总功率、噪声 | 1.0、1.0 | 环境配置 |
| Worker episode | 16 step | `episode_length` |
| Manager 间隔 | 4 step | `high_level_interval` |
| 切换惩罚 | 0.05 | `partition_switch_penalty` |

### 步骤 2.2：写实验卡片

```text
实验名称：main-default
研究问题：学习型 Manager 是否优于启发式 Manager？
训练 seeds：11, 29, 47, 61, 73
测试场景 seeds：10000 至 10499
训练步数：100000
主要指标：mean_sum_rate
约束指标：qos_satisfaction_rate、mean_min_user_rate、mean_jain_fairness、mean_partition_switches
所有方法共享：信道采样范围、总功率、噪声、RZF、测试场景
```

### 步骤 2.3：检查 HRL 环境

```bash
python -m pytest tests/test_staged_hrl.py tests/test_manager_training_env.py tests/test_evaluate_hrl.py -q
```

```python
from meta_hrl.envs import HierarchicalRSMAEnv
env = HierarchicalRSMAEnv()
observation, info = env.reset(seed=42)
observation, info = env.set_manager_action(0)
observation, reward, terminated, truncated, info = env.step(env.worker_action_space.sample())
print(reward, info["sum_rate"], info["partition"])
```

应看到 18 维 Worker 动作对应的有效 reward、分区和 `manager_action_required`。本阶段完成标志是实验卡片已写好，且一次 episode 能无异常结束。

## 3. 先补齐公平评估器

没有统一评估器，后面的性能差异无法解释。建议新增 `src/meta_hrl/training/evaluate_hrl_baselines.py`。

### 步骤 3.1：统一输出字段

逐 episode 保存：`method_name`、`training_seed`、`scenario_seed`、`episode_reward`、`mean_sum_rate`、`mean_min_user_rate`、`mean_jain_fairness`、`mean_total_qos_gap`、`qos_satisfied`、`partition_index`、`partition_switches`、`manager_decisions`、`manager_time_ms`、`worker_time_ms`。

不要只保存最终平均值。逐 episode 数据才能做配对检验、异常检查和后续重新汇总。

### 步骤 3.2：共享测试场景

第一版只需让所有方法使用相同的测试 seed：

```python
test_seeds = list(range(10_000, 10_500))
for episode_index, scenario_seed in enumerate(test_seeds):
    observation, info = environment.reset(seed=scenario_seed)
```

评估输出必须包含 `scenario_seed`。随机抽查一个 seed，比较不同方法的 `ranges_m`、`angles_rad`、`near_field_mask`；它们应完全相同。更稳妥的第二版是把场景参数保存为 JSON。

### 步骤 3.3：按顺序接入方法

1. `FixedPartitionManager + PPO Worker`；
2. `HeuristicPartitionManager + PPO Worker`；
3. `Manager PPO + 冻结 Worker`；
4. 随机分区 + PPO Worker；
5. 等功率/固定公共率等非学习基线。

固定、启发式和学习 Manager 的比较应尽量使用同一个 Worker checkpoint，或使用相同网络、训练步数和 seed 的 Worker。

### 步骤 3.4：写评估测试

- 固定 seed 时逐 episode 输出可复现；
- 各方法的 `scenario_seed` 列表相同；
- 必需字段存在且无 NaN/Inf；
- 动作、分区和资源约束始终合法；
- 2 个 episode 的 smoke test 能快速完成。

**本阶段完成标志：** 一次评估得到可读 JSON/CSV，并可以只用原始逐 episode 数据重新计算均值。

## 4. 建立基线，再训练主方法

### 步骤 4.1：最低基线集

| 编号 | 方法 | 状态 | 回答的问题 |
| --- | --- | --- | --- |
| B1 | Equal Power + RZF SDMA | 已有 | 与简单物理分配相比如何 |
| B2 | Fixed Common Power + RZF-RSMA | 已有 | 公共流的基础收益 |
| B3 | 固定分区 + PPO Worker | 已有 | 没有学习高层分组时如何 |
| B4 | 启发式分区 + PPO Worker | 已有 | 学习 Manager 是否优于规则 |
| B5 | 随机分区 + PPO Worker | 需补 | 分组收益是否只是随机性 |
| B6 | 学习 Manager + PPO Worker | 已有接口 | 主方法 |
| B7 | 启发式分区 + 搜索资源分配 | 需补 | 对比非学习型联合方案 |

B1-B6 是最低集合；B7 和小规模 oracle 用于提高说服力。

### 步骤 4.2：实现随机 Manager

在 `agents/high_level_policy.py` 增加符合 `PartitionManager` 协议的可复现随机 Manager。测试固定 seed 下输出一致，且索引始终在候选范围内，然后加入 `tests/test_hrl_policy_interfaces.py`。

### 步骤 4.3：训练 Worker

用 `train_staged_hrl_worker()` 先做 10,000 steps smoke run，再做正式 100,000 steps。`batch_size` 必须不大于 `n_steps`，且 `n_steps % batch_size == 0`；默认 256/64 已满足。

```python
from pathlib import Path
from meta_hrl.agents.high_level_policy import HeuristicPartitionManager
from meta_hrl.training import StagedHRLConfig, train_staged_hrl_worker

result = train_staged_hrl_worker(StagedHRLConfig(
    manager=HeuristicPartitionManager(), seed=11, total_timesteps=100_000,
    checkpoint_directory=Path("outputs/paper/02_heuristic_manager/seed_11/checkpoints"),
    log_directory=Path("outputs/paper/02_heuristic_manager/seed_11/logs"),
))
print(result.model_path)
```

### 步骤 4.4：训练 Manager

严格按以下顺序：

1. 固定/启发式 Manager 下训练并保存 Worker；
2. 加载 Worker；
3. 构造 `ManagerPPOConfig(worker=worker)`；
4. 调用 `train_manager_ppo()`；
5. 使用冻结 Worker + 新 Manager 评估。

当前仓库没有联合训练。论文中应称为 **staged training**，不要写成端到端联合 HRL。

**本阶段完成标志：** B3、B4、B5、B6 都有 checkpoint 和统一评估原始数据。先用 2 个 seed 排错，再扩大到 5 个 seed。

## 5. 运行第一张主结果表

这是决定是否继续的关键阶段。

### 步骤 5.1：正式 seed 方案

```text
训练 seed：11, 29, 47, 61, 73（至少 5 个）
每个模型测试场景：10000 至 10499（500 个）
每个 seed 训练步数：100000
```

训练 seed 衡量 PPO 训练不确定性，测试场景 seed 衡量无线布局不确定性；两者不能混用。

### 步骤 5.2：生成主表

在 seed 层面汇总，不要把所有 episode 直接当成独立训练样本：

| 方法 | mean sum rate | QoS 满足率 | 最小用户率 | Jain 公平性 | 切换次数 | 推理时间 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| B1-B6 | ... | ... | ... | ... | ... | ... |

主要指标是和速率；其余指标用于检查是否通过牺牲公平性、QoS 或频繁切换换取速率。

### 步骤 5.3：做配对检验

对主方法 B6 和最强对手 B4 按相同训练 seed 配对：

```text
difference_i = metric(B6, seed_i) - metric(B4, seed_i)
```

对差值计算 95% bootstrap CI 和双侧 sign-flip p 值。CI 不跨 0 且 p<0.05 才能称“统计显著优于”；否则只能称“有正向趋势”。

### 步骤 5.4：继续/停止判断

| 结果 | 决策 |
| --- | --- |
| B6 稳定优于 B4，且 QoS/公平性未明显恶化 | 进入消融和泛化 |
| B6 速率相近但切换更少或 QoS 更好 | 改写为性能-稳定性折中后继续 |
| B6 不如 B4 | 暂停扩展，检查奖励、Manager 训练和分区 |
| 学习方法都不如物理基线 | 先检查信道尺度、QoS、奖励和动作投影 |

**本阶段完成标志：** 有主表、逐 episode 原始数据、seed 统计报告和一段不夸大结论的文字。

## 6. 做 HRL 核心消融

每次只改变一个因素，其他参数、seed 和测试场景保持不变。

| 顺序 | 因素 | 取值 | 问题 |
| --- | --- | --- | --- |
| A1 | Manager 类型 | 固定/启发式/学习/随机 | 学习分组是否有用 |
| A2 | 高层间隔 | 1、2、4、8 | 双时间尺度是否必要 |
| A3 | 切换惩罚 | 0、0.01、0.05、0.1 | 是否靠频繁切换换性能 |
| A4 | 预编码器 | MRT、RZF | 是否依赖 RZF |
| A5 | 候选数量 | 前 10/30/100/203 | 复杂度-性能折中 |
| A6 | 公共流结构 | 单公共流/组公共流 | 组 RSMA 的价值 |

优先做 A1-A3；每项正式结果至少 5 个 seed。筛选阶段可用 2 个 seed，正式报告不能只用 2 个。

以 A2 为例：只修改 `high_level_interval`，保持 `episode_length=16`；每个取值重新训练；输出和速率、QoS、切换次数、Manager 决策次数和推理时间；绘制均值 + 95% CI。不能修改 interval 后继续使用旧 checkpoint，除非研究迁移。

同时记录每个候选分区被选择的频率、组大小、切换发生的场景，形成解释性分析。

**本阶段完成标志：** 能用数据解释为什么采用当前分层间隔、切换惩罚和 Manager 方案。

## 7. 补强非学习基线和小规模 oracle

### 步骤 7.1：先做搜索基线

完整 WMMSE 成本高且易在 RSMA 约束上出错。先实现清晰的启发式+搜索：

1. `select_heuristic_partition()` 选分区；
2. 等功率动作作为初始值；
3. 对 Worker logits 做固定次数随机/坐标搜索；
4. 每个动作调用 `project_group_action()` 保证可行；
5. 用 `group_rsma_rates()` 计算目标并保留最好值。

可放在 `rsma/group_baselines.py`，显式传入 seed 和搜索次数。

### 步骤 7.2：测试搜索基线

检查总功率、禁用槽位、公共率预算、固定 seed 可复现，以及搜索结果不差于初始化动作。

### 步骤 7.3：做小 K oracle

只在 K=4 或 K=6、少量场景运行：枚举所有候选分区，对每个使用相同资源搜索器，保存最优值、分区、HRL 值及差值。论文中标注为 small-scale oracle/reference，不宣称可实时运行。

**本阶段完成标志：** 至少一个搜索基线在相同场景上比较，并有 HRL 到 oracle 的差距。

## 8. 做扩展性、鲁棒性和现实性实验

主表和消融成立后再做这一阶段。

### 步骤 8.1：规模扩展顺序

1. 天线数 `M=64,128,256`，先保持 K=6；
2. 用户数 `K=4,6,8`，先保持 M=128；
3. 近场比例：全远场、混合、全近场；
4. QoS/噪声：宽松、中等、严格。

每改一个设置，先运行：

```bash
python -m pytest tests/test_task_sampler.py tests/test_observation_encoder.py tests/test_group_rsma.py -q
```

再随机完成一个 episode，确认动作维度为 `3K`、观察维度正确、候选分区合法。

### 步骤 8.2：处理 K 增大后的分区爆炸

依次实现：最大组大小限制；按启发式分数预筛选前 N 个；把候选数量写入结果；比较 N=10/30/100/全候选的性能和时间；论文中说明全枚举仅用于小 K。

### 步骤 8.3：加入不完美 CSI

最小模型：`H_est = sqrt(1-tau^2) * H_true + tau * E`，`tau=0,0.05,0.1,0.2`。策略观察和预编码使用 `H_est`，真实速率使用 `H_true`；`tau=0` 必须退化为现有结果。建议扩展 `RSMAScenario` 保存真实/估计信道，使用独立 RNG，并补测试。不能只给 observation 加噪声而用干净信道预编码。

### 步骤 8.4：时变信道（可选）

让 Worker step 更新小尺度衰落或用户位置，Manager 按慢间隔更新；记录速度、块间相关性和 CSI 延迟。做不到时，在论文限制中明确“块静态信道”。

**本阶段完成标志：** 至少覆盖两种规模、两种近远场比例、两档 QoS/噪声和三档 CSI 误差中的合理子集，并记录推理时间和候选数。

## 9. Meta-PPO 只能二选一

### 路线 A：作为严谨消融（推荐）

保留 learned/raw 配对结果，在消融表报告差异和 CI；说明当前 context 是确定性 7 维 transition 统计，学习分支只是后续 MLP 表示；在限制部分写明未观察到稳定 few-shot 收益；不把 Meta-RL 放入标题和核心贡献。

### 路线 B：重做后作为核心贡献

明确要推断的 task 变量；加入真实任务参数 oracle 和 no-context baseline；采用 transition 集合编码器、GRU 或 Transformer；在至少两个独立 OOD 分布上做 paired multi-seed 适应曲线。只有相对 raw context 在两个 OOD 设置上都有稳定、显著的正收益，才选择此路线。

## 10. 统计、图表和论文写作

### 步骤 10.1：最低统计协议

- 每个学习方法至少 5 个训练 seed，推荐 8 或 10 个；
- 每个模型在相同的 100 个以上测试场景评估，推荐 500 个；
- 图中显示均值和 95% CI，表中至少给均值和标准差；
- 主方法与最强基线使用 seed 级配对 bootstrap 和双侧检验；
- 保存 JSON/CSV 原始数据，不只保存图；
- 使用预先定义的最终 checkpoint 或验证规则，不能挑最好 checkpoint。

### 步骤 10.2：建议图表顺序

1. Table I：系统和训练参数；
2. Table II：主方法与基线；
3. Fig. 1：系统及 Manager/Worker 时序；
4. Fig. 2：多 seed 收敛曲线；
5. Table III/Fig. 3：A1-A3 消融；
6. Fig. 4：规模、近远场比例或 QoS 扫描；
7. Table IV：复杂度和推理时间；
8. 可选：Meta context 或 CSI 误差图。

每张图注明方法数、训练 seed 数、测试场景数、误差条含义、参数和是否共享场景。

### 步骤 10.3：测量开销

记录参数量、训练时间、环境交互步数、Manager/Worker 单次决策时间、是否包含分组/预编码/速率计算以及软件硬件版本。在线延迟使用固定硬件、batch=1、预热后多次测量并报告均值和分位数；训练成本和在线成本分开写。

### 步骤 10.4：论文贡献边界

建议只写三项并逐项提供证据：

1. 混合近远场大规模 MIMO 的组 RSMA 双时间尺度决策建模；
2. 规范用户分区与固定公共流槽位的固定维度可行动作表示；
3. 包含强基线、消融、扩展性、鲁棒性和开销统计的可复现实证评估。

不要把“用了 PPO”“实现了 203 个分区”“写了代码”单独称为算法创新。Meta 分支无收益时不要声称它带来性能提升。

## 11. 推荐执行顺序与里程碑

| 阶段 | 工作 | 交付物 |
| --- | --- | --- |
| M1 | 测试、实验卡片、统一评估器 | 逐 episode JSON/CSV |
| M2 | B1-B5 基线和 smoke run | 基线原型表 |
| M3 | B3/B4/B6 五 seed 正式训练 | 主结果、CI、继续/停止决定 |
| M4 | A1-A3 消融 | HRL 归因图表 |
| M5 | 搜索/oracle、复杂度、规模和 CSI 子集 | 强基线、开销、鲁棒性表 |
| M6 | Meta 决策、论文图表和复现包 | 内部评审稿 |

每次实际运行都遵循：短 smoke test -> 确认输出目录 -> 一个 seed -> 检查 JSON/指标/场景 seed -> 批量剩余 seed -> 自动汇总 -> 记录 commit。

## 12. 投稿前检查表

- [ ] 标题、摘要、贡献与数据一致，没有宣称未验证的 Meta-RL 增益。
- [ ] 所有方法共享测试场景、功率、噪声、QoS、CSI 假设和预编码器。
- [ ] 至少 5 个基线，其中至少 2 个非学习型。
- [ ] 学习 Manager 与固定/启发式 Manager 使用相同 Worker 条件。
- [ ] 主结果有至少 5 个训练 seed、统一测试场景、CI 和配对检验。
- [ ] 已完成 Manager 类型、更新间隔、切换惩罚消融。
- [ ] 已报告候选数、参数量、训练成本和在线推理时间。
- [ ] 已完成至少一种规模变化和一种现实扰动（推荐 CSI 误差）。
- [ ] 每张图可追溯到配置、代码版本、checkpoint 和原始数据。
- [ ] 已写清块静态/理想 CSI、两阶段训练、分区扩展性和 Meta 负结果等局限。

完成第 1 至第 7 节并通过检查后，项目具备向 SCI 二区相关期刊投稿的扎实基础。若主方法不能在强基线上取得可解释优势，应优先改进问题定义或方法，而不是继续增加实验维度。
