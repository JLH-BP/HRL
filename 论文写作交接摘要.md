<!-- 可直接粘贴到新聊天，为 META_HRL 论文撰写和实验提供上下文。 -->
# META_HRL 论文写作交接摘要

> 使用方式：将“可直接粘贴给新聊天的摘要”一节完整复制到新的 Codex/ChatGPT 对话中，再说明当前希望完成的具体任务。

## 可直接粘贴给新聊天的摘要

我正在撰写一篇拟投 MDPI 通信/无线方向期刊的论文，导师要求我在 **2026 年 9 月 22 日** 提交初稿。项目仓库位于 `D:\Users\Study\code\META_HRL`，Python 项目名为 `meta-hrl`。请帮助我在不重写现有代码框架的前提下，完成论文主线梳理、必要实验、图表和初稿写作。请先阅读仓库中的 `README.md`、`代码操作指南.md`、`具体工作.md`、`code_list.md` 及相关源代码和已有输出；不要假定旧任务框架文档一定反映当前状态。

### 研究问题与论文主线

研究对象是混合近场/远场大规模 MIMO-RSMA 下行系统：默认 128 阵元 ULA、6 个单天线用户、28 GHz、混合近/远场莱斯信道。需要联合决定用户分组、私有流功率、组公共流功率和组内公共率，同时考虑总速率、公平性、QoS 缺口和分组切换代价。

拟定论文主线是：现有混合场用户调度文章通常先识别/确定近场用户，再在此基础上服务远场用户，即近场优先的顺序式调度；本工作希望让所有近场与远场用户直接进入同一个候选分区空间，由 HRL 联合决定分组和 RSMA 资源分配。论文需要证明的不是“使用了 PPO”，而是：**联合调度是否比近场优先顺序调度、固定分组和启发式分组更有效，或者能给出明确的速率-QoS-稳定性折中。**

建议英文标题暂定为：

`Joint Hierarchical Reinforcement Learning for User Grouping and RSMA Resource Allocation in Mixed Near- and Far-Field Massive MIMO Systems`

### 当前已实现的代码基础

- `channel/`：ULA 几何、Rayleigh 近远场判定、近场球面波、远场平面波、莱斯信道及数值验证。
- `rsma/`：单公共流和组公共流 RSMA、MRT/RZF 预编码、SINR/速率、资源约束和物理基线。
- `grouping/`：规范化用户分区、候选分区枚举、启发式分组。默认 K=6 时有 203 个无标签规范分区。
- `envs/rsma_env.py`：单步 `OneStepRSMAEnv`，用于 Flat PPO；动作为 13 维 logits。
- `envs/hierarchical_env.py`：多步 `HierarchicalRSMAEnv`。Manager 选 `Discrete(203)` 分区，Worker 选固定 `3K=18` 维 logits。调用顺序必须是 `reset() -> set_manager_action(partition_index) -> step(worker_action)`。
- 组 RSMA 使用固定槽位设计：每个非单例组以该组最小用户编号绑定公共流槽位，禁用槽位强制为 0，因此分组变化不会改变 Worker 动作维度。
- `WorkerTrainingEnv`：用固定或启发式 Manager 训练 PPO Worker。
- `ManagerTrainingEnv`：冻结 Worker，将高层区间累计奖励用于训练离散 PPO Manager。
- 当前 HRL 是 **两阶段（staged）训练**：先训练 Worker，再冻结 Worker 训练 Manager；没有实现端到端联合微调。论文必须如实说明。
- `training/train_flat_rl.py`、`train_hrl.py`、`evaluate.py`、`evaluate_hrl.py`：Flat PPO、两阶段 HRL 的训练/评估入口。
- `agents/high_level_policy.py` 已有 `FixedPartitionManager` 和 `HeuristicPartitionManager`；还没有确认实现“近场优先顺序 Manager”或随机 Manager。
- `tests/` 有信道、RSMA、环境、HRL、Meta-PPO、CLI 和分析测试。先运行局部测试，修改接口后运行 `python -m pytest`。

### 当前尚未完成、但为论文主线最需要做的事

1. **实现明确的近场优先顺序调度基线。** 应新增一个 Manager（建议放 `agents/high_level_policy.py`），根据 `scenario.near_field_mask` 先处理近场用户、再处理远场用户，并最终返回一个合法候选分区索引。其具体规则必须尽量依据导师指定的相关文献；如果没有精确复现文献，只能称为“near-field-first sequential baseline”，不能声称完整复现某方法。
2. **实现统一的 HRL 基线评估器。** 建议新增 `training/evaluate_hrl_baselines.py`。不同方法必须使用相同 `scenario_seed` 列表，例如 `10000...10499`，并逐 episode 保存 JSON/CSV：方法名、训练 seed、场景 seed、reward、sum rate、最小用户率、Jain 公平性、QoS gap/QoS 是否满足、分区索引、切换次数、Manager 决策次数、推理时间。
3. **跑主比较。** 至少比较：等功率 RZF-SDMA、固定公共功率 RZF-RSMA、固定分区 + PPO Worker、近场优先顺序 Manager + PPO Worker、启发式 Manager + PPO Worker、学习 Manager + PPO Worker。若时间紧，优先后四种，其中“近场优先 vs 联合学习 Manager”最关键。
4. **做核心消融。** 优先做 Manager 类型（固定/顺序/启发式/学习）和高层更新间隔 `high_level_interval=1,4,8`；若有余力，再扫切换惩罚 `0,0.05,0.1`。
5. **做一项体现混合场主题的场景实验。** 例如近场用户比例为 0%、约 50%、100%；确保结论不是只在某个单一混合比例成立。
6. **只在主比较成立后** 再做 CSI 误差、用户/天线规模扩展、搜索/WMMSE 基线、时变信道等增强实验。

### 已有 Meta-PPO 结论和写作边界

项目实现了 task-conditioned Meta-PPO：`MetaTaskSampler` 区分 train/validation/OOD 任务，`TaskContextBuffer` 按 task ID 隔离 transition，`MetaContextEncoder` 从近期 transition 得到 7 维确定性 context，另有 learned MLP feature branch。

已有 `outputs/benchmark_8seeds/` 的 8 seed、100k timestep paired benchmark 结果显示：**learned-context 在 validation 与 OOD 上均弱于 raw deterministic context**，support episodes 增至 10 也没有清晰 few-shot 提升。因此：

- 不要把 Meta-RL 放进标题或摘要的核心性能贡献；
- 可以作为消融/负结果/未来工作保留；
- 主论文应聚焦 HRL 联合用户分组与资源分配。

### 推荐主实验协议

- 第一轮默认环境：`M=128`、`K=6`、RZF、总功率 1、噪声 1、`episode_length=16`、`high_level_interval=4`、`partition_switch_penalty=0.05`。
- 正式训练 seed：至少 `11,29,47,61,73`；若算力允许扩展到 8 或 10 个。
- 每个模型用完全相同的测试场景 seed 评估，最少 100 个、推荐 500 个（例如 `10000...10499`）。
- 主指标：平均和速率（mean sum rate）。
- 约束/解释指标：QoS 满足率、最小用户速率、Jain 公平性、平均分区切换次数、Manager/Worker 推理时间。
- 对主方法和最强基线按训练 seed 做配对 bootstrap 95% CI 与双侧 sign-flip 检验。不要将大量测试 episode 当作独立训练重复样本。

### 论文方法与贡献可采用的表述

中文贡献草稿：

1. 将混合近远场大规模 MIMO 的用户分组与组 RSMA 资源分配建模为双时间尺度层次决策问题，高层 Manager 在全体用户组成的候选规范分区中进行联合选择，低层 Worker 执行连续资源分配。
2. 提出（或采用并系统化实现）规范用户分区和固定组公共流槽位表示，使不同组数和组大小下 Worker 始终保持固定 `3K` 维可行动作空间，避免分组标签置换和变长动作问题。
3. 构建在共享场景、强基线、多 seed、统计检验和混合场比例下的可复现实证评估，比较联合调度和近场优先顺序调度的性能与开销。

不要宣称“首次提出”“显著优于所有方法”“端到端联合训练”或“Meta-RL 带来提升”，除非后续数据严格支持。

### 建议论文结构

1. Introduction：指出顺序近场优先调度的结构限制，以及联合调度动机。
2. Related Work：近场/混合场通信、RSMA 资源分配、用户分组、无线 HRL；必须查真实文献，不能编造引用。
3. System Model and Problem Formulation：信道、RSMA、约束、目标、近远场判定、CSI 假设。
4. Proposed Joint HRL Framework：候选分区、Manager/Worker、固定槽位、动作投影、staged training、复杂度。
5. Experimental Setup：参数、基线、训练/测试 seed、共享场景协议、指标和统计方法。
6. Results and Discussion：主表、Manager 类型消融、混合场比例、间隔/切换惩罚、开销；诚实讨论失败或限制。
7. Conclusion：只重申被结果支持的结论。

### 截止时间和推荐节奏

导师要求 2026-09-22 初稿，时间很紧。不要重写代码框架，也不要现在重做 Meta-RL。优先顺序：

1. 立刻实现顺序 Manager 和统一评估器，并加 smoke tests；
2. 用 2 个训练 seed、50 个测试场景、10k/20k steps 跑通所有方法；
3. 检查共享场景、合法分区、有限指标和输出 JSON；
4. 扩到 5 个训练 seed、100k steps、100-500 个测试场景跑主表；
5. 做 Manager 类型和近场比例实验；
6. 同时开始写 Introduction、System Model、Method、Experimental Setup，这些不依赖最终数值；
7. 初稿允许保留“待补实验”，但必须有清晰方法、至少一张主表和一项消融/场景图。

### 已有文档和输出

- `README.md`：当前项目概况与 Meta benchmark 结论。
- `代码操作指南.md`：代码操作、训练和实验指导；当前最完整的运行文档。
- `具体工作.md`：新手可执行的 SCI 二区实验路线和验收标准。
- `code_list.md`：模块/API 清单。注意该文档可能仍引用旧文件名；以实际文件和源代码为准。
- `outputs/benchmark_8seeds/`、`outputs/sensitivity/`：已有 Meta 实验工件，不应覆盖。

### 当前的个人约束

我同时处于秋招阶段，时间和精力有限。请建议最小可行的实验和写作路线，避免建议我重写项目、加入大规模新算法或在 9 月 22 日前做不必要的工程重构。帮助我时请明确区分“必须完成以交初稿”和“可作为后续增强”的工作。

## 新聊天中的首个请求建议

将摘要粘贴后，可接着发送：

> 请先审查当前 `high_level_policy.py`、`worker_training_env.py`、`manager_training_env.py`、`evaluate_hrl.py` 和相关测试，设计并实现一个可复现、物理/分区合法的 near-field-first sequential Manager 基线。请不要修改现有 HRL 核心物理层；新增测试，并先运行局部测试验证。

后续再单独请求：

> 请基于现有评估代码，实现共享 `scenario_seed` 的 HRL 基线评估器，输出逐 episode JSON/CSV，并为论文主表提供 seed 级汇总和配对统计输入。

