# Defines HRL packaging, optional dependencies, and test configuration.
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hrl"
version = "0.1.0"
description = "HRL Python project"
readme = "README.md"
dependencies = [
    "gymnasium>=1.0",
    "matplotlib>=3.7",
    "numpy>=1.24",
    "PyYAML>=6.0",
    "stable-baselines3>=2.4",
]
requires-python = ">=3.10"
authors = [
    { name = "META_HRL contributors" },
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
hrl = "hrl.cli:main"
# Retained so existing experiment scripts keep working after the package rename.
meta-hrl = "hrl.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
